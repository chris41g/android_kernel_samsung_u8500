#
# Cypress touchkey configuration
#

config KEYBOARD_NEXTCHIP_TOUCH
	tristate "Nextchip touchkey support"
	default n
	help
	  Say Y here to enable the cypress touchkey.

	  To compile this driver as a module, choose M here.
